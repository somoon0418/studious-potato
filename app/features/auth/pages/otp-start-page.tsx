import type { Route } from "./+types/otp-start-page";

export const meta: Route.MetaFunction = () => {
  return [{ title: "Otp Login | wemake" }];
};

export default function OtpStartPage() {
  return <div></div>;
}
